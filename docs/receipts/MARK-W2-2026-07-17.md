# MARK-W2 build receipt — `mark-garden` (2026-07-17)

**Lane:** MARK-W2 builder (foreground). **Branch:** `feat/mark-w2` off `origin/master`
@ `2064500f83` (master carrying MG-W1 merged — mark table + `SeasonLedgerStore.Mark.cs` +
`MarkKind`/`MarkAgeRules`/`MarkBeatQueue`/`MarkCopy` + `CCVars.SolreignMark`). **Spec:**
`orch-ops docs/specs/MARK-SPEC-2026-07-17-DRAFT.md`, wave MG-W2 (§10). No merges, no pushes —
intake is the orchestrator's.

## What was built (spec-vs-built)

| Spec item (MG-W2) | Built | Delta from spec |
|---|---|---|
| `mark_garden.yml` (garden + base + 12 stage prototypes, PointLight steps) | `Resources/Prototypes/_Solreign/Entities/mark_garden.yml` — `SolreignMarkBase` (abstract) + `SolreignMark{Sapling,Lamp,Plate}{0..3}` + `SolreignMarkGarden`. Lamp stages step PointLight radius/energy/color cold-white → settled amber; plate stages deepen a patina sprite tint. | The base is **component-explicit, not `BaseStructure`-parented** — T3 resolution below. Garden name/description authored inline (the `signage.yml` EntityLocData-fallback idiom) AND mirrored as the C5 keys in `mark.ftl` (the copy contract source; duplication accepted, receipt-noted). |
| `SolreignMarkGardenComponent` / `SolreignMarkComponent` | Garden comp in **Content.Shared** (`_Solreign/PlayerDelight/Mark/`) because it appears in YAML (the house rule: every YAML-referenced component is shared — verified against Contracts/Wingmates/FirstShift); carries the `SlotOffsets` DataField (default 24-slot 6×4 bed from `MarkGardenLayout`). Mark comp stays **Content.Server** (attached in code at spawn, never YAML, never networked — owner GUID + planted name never leave the server). | Spec §3.2 said "server-side component" for both; the garden comp moved shared as a load-bearing correction (a server-only component in YAML fails the client prototype load). Not networked, no behavior client-side. |
| `SolreignMarkGardenSystem` (projection §3.3, fallback §4.3, verbs, examine, C6) | `Content.Server/_Solreign/PlayerDelight/Mark/SolreignMarkGardenSystem.cs` — StationPostInit materialize-and-project (gate-before-await station guard, post-await `Deleted()` re-checks, corrupt-row skip, no-orphans spawn discipline); §4.3 fallback (deterministic lowest-uid beacon pick, fixed 12-tile candidate ring, space/Impassable tile checks, delete-on-anchor-failure); 3 planting alt-verbs (session + non-ghost gated, InRound-gated claim — the review-F1 rule, gate-before-await per-account in-flight set, C1 kind-true confirm popup+chat, quiet already-claimed refusal); examine = stage line + owner/stranger suffix (owner name from the component cache, no store round-trip). | **C6 overflow DELIVERY deferred to MG-W4** (boundary decision): the C6 line fires "on the owner's next spawn" (spec §3.3.4) — that is the `PlayerSpawnCompleteEvent` hook MG-W4 owns wholesale; splitting one spawn-hook between waves would hand W4 a collision surface. The C6 templates + keys ship NOW and are existence-tested. Also NOT here by wave law: `NotifyMarkPlanted` (W3 — call site commented at the exact line), return/nudge beats (W4). |
| `mark.ftl` | `Resources/Locale/en-US/_solreign/mark.ftl` — the §8 pack **verbatim** (12 stage lines, kind words, C1 kind-true + generics, C2 kind-true + generics, C3/C4 suffixes, C5 garden, C6 overflow, C7 nudges) + 3 planting-verb labels (§2's fixed menu) + the already-claimed refusal line. | Two W2-authored additions beyond the grk pack, both in-voice and PG-13: `solreign-mark-verb-*` ("Plant a sapling" / "Light a lamp" / "Seat a name-plate", spec §2 verbatim as verbs) and `solreign-mark-plant-already` ("The ledger shows one mark for you already. One is the number. It is doing fine."). `MarkCopy` gained the matching `VerbKeyFor` + `AlreadyClaimedKey` (additive, Mark-lane-owned file). |
| `CCVars.SolreignMark.cs` | Already landed in W1 (pulled forward, per the W1 receipt). W2 is the first consumer: `enabled` gates everything, `slots` caps the projection read. `return_beat_enabled` remains unread until W4. | None. |
| Integration tests (plant-once / projection / dormancy) | `Content.IntegrationTests/Tests/_Solreign/MarkGardenIntegrationTest.cs` — 6 scenarios, detailed below. | Adds fallback-placement, owner-only-examine, and real-round-restart wiring scenarios beyond the named three. |
| Delegation plumbing | New partial `SeasonLedgerSystem.Mark.cs` — thin store delegations (claim/get/get-all + the W4 stamps), the exact `SeasonLedgerSystem.FirstDeath.cs` access pattern ("reach the ledger through this system, never a second store instance"). | New file, zero collision. The lane brief's `SeasonLedgerDbPath.Resolve` house law is honored *through* this: the one store `SeasonLedgerSystem` already builds resolves through it. |
| Unit tests | New `MarkProjectionTests.cs` (12 tests: slot-layout determinism/uniqueness/modulo law, closed 12-id prototype table, aged-fixture stage→prototype composition) + `MarkCopyTests.cs` extended with the W1-owed locale harness (6 tests: key existence both directions, closed vocabulary, **$name-only-in-owner-suffixes denylist** — rail 5 as a test, kind-true unit words, 140-char stage budget). | The W1 receipt's "W2 owes" debt is discharged. |

Also touched: `Resources/_Solreign/orphan_allowlist.yml` — all 14 mark prototypes declared with
reasons (C#-only spawns by design; the orphan-reachability gate caught them, correctly).
`SeasonLedgerStore.Mark.cs` gained one `internal SetMarkPlantedUtcForTests` seam (rewinds
`planted_utc`+`last_visit_utc` on a genuinely-claimed row) so aged projection fixtures go through
the REAL atomic claim rather than hand-inserted rows; no production caller, documented as such.

## Placeholder sprite table (per-prototype, all upstream stock — NO AI art, per lane rails)

| Prototype | Sprite source (upstream CC-BY-SA stock) | State / stage read |
|---|---|---|
| SolreignMarkSapling0 | `Structures/Furniture/potted_plants.rsi` | `plant-04` (sparse sprouts) |
| SolreignMarkSapling1 | `Structures/Furniture/potted_plants.rsi` | `plant-02` (thin young tree) |
| SolreignMarkSapling2 | `Structures/Furniture/potted_plants.rsi` | `plant-08` (full small tree, fruiting) |
| SolreignMarkSapling3 | `Structures/Furniture/potted_plants.rsi` | `plant-11` (broad permanent-looking tree) |
| SolreignMarkLamp0..3 | `Objects/Misc/Lights/lampgreen.rsi` (base + unshaded on-layer) | stage via PointLight: radius 1.5→3.0, energy 1.0→2.2, color `#E8F4FF`→`#FFD083` (cold white → settled amber) |
| SolreignMarkPlate0..3 | `Structures/Wallmounts/signs.rsi` | `medium_blank`; stage via sprite tint `#FFFFFF`→`#F2E8C9`→`#E0CD9E`→`#C9AF6E` (patina) |
| SolreignMarkGarden | `Structures/Hydroponics/misc.rsi` | `soil` (garden bed) |

Sapling states were picked by eye from a rendered montage for a legible small→large growth read.
Custom acid-green stage-differentiated RSIs remain a `TODO(SOLREIGN-ART)` sprite-factory-lane
task (header comment in `mark_garden.yml`), NOT a blocker.

## [needs verification] tag resolutions (W2-owed)

- **T3 — §4.1 parent choice + Pullable (the W1 receipt's keep-or-suppress, task order: SUPPRESS):**
  **RESOLVED — suppressed, by not parenting.** Robust prototype composition cannot subtract a
  parent's component, so `SolreignMarkBase` takes the spec's own escape hatch ("build the
  component list explicitly instead of parenting"): Transform(anchored, noRot) + Clickable +
  InteractionOutline + Physics(Static) + one small `hard: false` fixture. No Pullable, no
  Damageable, no Destructible, no Anchorable, no Tag(Structure), no construction, no containers,
  no ActivatableUI. The keepsake cannot be dragged, damaged, unanchored, or renamed — and the
  fixture is non-hard so it never blocks a hallway tile. Integration-pinned (`Anchored` asserted
  on every seat/projection).
- **§4.2 `ExaminedEvent` examiner field:** **VERIFIED** — `ExaminedEvent.Examiner`
  (`Content.Shared/Examine/ExamineSystemShared.cs:434`), resolved to a session via
  `IPlayerManager.TryGetSessionByEntity`; sessionless examiners take the stranger path. Owner
  suffix uses the name cached on the component at projection time — no async in examine, as
  specced.
- **§4.3 tile-occupancy check:** **RESOLVED** — `TurfSystem.GetTileRef`/`IsSpace` (the exact
  predicates `SolreignWingmateBeaconMapPlacementIntegrationTest` asserts as "safely placed") +
  `TurfSystem.IsTileBlocked(tile, CollisionGroup.Impassable)` for the fallback-garden tile, +
  anchor-or-delete after every spawn (the `SolreignGhostCabinetsRule` no-orphans discipline).
  Atmos-safety is NOT checked at runtime (a garden beside the beacon inherits the beacon's
  test-asserted safe placement; MG-W5's map-placement test should clone the beacon test's full
  atmos battery for map-placed gardens).
- **§7 station re-init in integration tests:** **RESOLVED** — scenarios drive the system's own
  StationPostInit path via an internal `MaterializeAndProjectForTests(station)` seam (house
  ForTests idiom; identical code path, CVar gate included), plus one REAL
  `GameTicker.RestartRound()` scenario proving the actual event subscription fires safely
  end-to-end. The pool's default map is tiny, so placement scenarios lay real floor tiles first
  (the map-health scorecard's `SetTile` lay-a-new-tile idiom) — the tests pin the system's law,
  not the fixture map's floor plan.

## Integration scenarios (all green)

1. **Dormant_ShipPosture_IsZeroBehavior** — asserts the CVar ships FALSE, then: no garden (not
   even fallback), no projections, and a plant attempt against a physically-present garden never
   touches a row. The ship posture is the tested posture.
2. **Plant_OnceEver_OneRowOneStageZeroEntity_SecondAttemptRefused** — real connected session:
   one row (kind pinned), one anchored stage-0 entity, correct prototype; a second attempt with a
   different kind changes neither row nor entity count.
3. **Projection_SeededAgedRows_StageMatchedPrototypesAtDeterministicSlots** — three genuinely
   claimed rows aged 30d/3d/0d via the test seam → stage 3/1/0 prototypes, each at exactly
   `gardenTile + SlotOffset(row.slot_index)` (residue-proof: asserts against real slot indices).
4. **Examine_OwnerGetsTheirName_StrangerNever** — owner examine carries the public stage line +
   C3 suffix with the character name; a sessionless examiner gets the stage line + C4, name
   absent (rail 5 pinned at the ECS level, on top of the copy-pack denylist test).
5. **FallbackPlacement_NoMapGarden_SpawnsBesideTheWingmateBeacon** — exactly one garden, anchored,
   same grid, within the fixed candidate ring, non-space tile.
6. **RealRoundRestart_EventWiring_NeverThrows_NoOrphans** — a real `RestartRound()` with the
   feature LIVE on a map with no beacon: clean start, nothing projected, no orphans (the log-once
   skip path).

## Verification evidence (Release, this branch)

| Gate | Result | Baseline |
|---|---|---|
| `dotnet build Content.Tests -c Release` | 0 errors | — |
| `dotnet build Content.IntegrationTests -c Release` | 0 errors | — |
| `dotnet build Content.YAMLLinter -c Release` | 0 errors | — |
| Content.Tests full suite | **2419 pass / 0 fail / 3 skip** | master 2401/3skip + exactly the 18 new/extended Mark tests (12 `MarkProjectionTests` + 6 `MarkCopyTests` locale extensions) |
| IntegrationTests `~Solreign` | **319 pass / 0 fail / 1 skip** (7m59s, full clean run) | baseline 313/1skip + the 6 new `MarkGardenIntegrationTest` scenarios |
| GameMapsLoadableTest | **246 pass / 0 fail** | 246 |
| Content.YAMLLinter | **No errors found** | no errors |

*Verification provenance:* the original builder session hit a machine-load infrastructure abort
mid-`~Solreign` run (300 passed, run killed — not a test failure). The MARK-W2 recovery lane
(2026-07-17) committed the finished work as-is (zero code changes needed) and re-ran the FULL
battery above cleanly, one suite at a time, on a quiet machine.

## Flags for the orchestrator

1. **WORKTREE-MAP registration** — this lane did not edit the OPS canonical tree; add the
   `~/AI/solreign-trees/mark-w2` / GAME / `feat/mark-w2` row on the next OPS pass.
2. **W3 owes:** the five §3.6 first-shift edits + `NotifyMarkPlanted` wiring (call site is
   commented in `PlantAsync`, immediately after the C1 delivery).
3. **W4 owes:** the `PlayerSpawnCompleteEvent` hook — return beats (C2), first-return nudge (C7),
   **and C6 overflow delivery** (moved here from W2, see boundary decision above); templates,
   keys, `MarkBeatQueue`, and the ledger stamps all already shipped.
4. **W5 note:** map-placed gardens should get the beacon placement test's full atmos battery
   (runtime fallback only checks space/blocked — see the §4.3 tag resolution).
5. **Council kill-test rider (spec §7):** once John flips `solreign.mark.enabled`, collect memo
   test 2 (≥3/5 returners walk to their mark within 5 min of their second visit, unprompted).
6. **C5 duplication:** the garden's name/description live inline on the prototype AND as
   `solreign-mark-garden-name/-desc` in mark.ftl; if the copy ever changes, change both (the
   copy test pins the ftl side only).
