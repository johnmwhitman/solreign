You are reviewing an actual code diff for a Space Station 14 fork (SOLREIGN, C#/Robust Toolbox engine). You previously specced this exact feature for me (the "Acid Storm" atmospheric station event) — I implemented against your spec. Now review the real diff below for correctness bugs and whether I actually followed your own verdict. You have no other context than this diff — it's complete.

CONTEXT RECAP (in case this is a fresh context for you): PROVIDENCE is a sinister-corporate AI announcer, PG-13, unsettling-corporate not horror. Your verdict was: (1) ONE opening screen-fx sting only, no repeated pulsing; (2) zero mechanical damage this wave; (3) text-only all-clear (no VO replay); (4) CVar gated via `ForceEndSelf` inside `Started()`; (5) double-start is accepted as harmless (no shared mutable state to restore, unlike the sibling Solar Flare event which restores flickered APCs); (6) ghosts/observers DO see the broadcast screen-fx (unlike the OTHER wow-wiring feature, a personal address beat, which must never hit ghosts) and mid-round-join players may simply miss the opening beat, both acceptable for weather; (7) reuse the existing `SolreignSporeParticleComponent`/system verbatim for ambient motes, just a new differently-tinted, longer-lived entity prototype, not a new particle mechanic.

Full diff (7 files, ~365 insertions):

```diff
PASTE_DIFF_HERE
```

YOUR TASK: Review this diff against your own verdict + failure-mode matrix from before, and fresh-eyes for anything new. Specifically:
1. Does `SolreignAcidStormRule.Started` actually match your verdict point-by-point (single sting, zero damage, no Ended override, ForceEndSelf-on-CVar-off placed correctly before any side effects run)?
2. `weight: 3` in `game_rules_weather.yml` — you suggested ~6-8 "between rare and normal"; I went lower (rare, per the ORIGINAL mission brief's explicit "weight it rare" instruction, which takes precedence over your suggested range since the mission is the actual requirement, not your suggestion). Any issue with weight 3 specifically vs the siblings (SolarFlare=8, SporeDrift=6) being noticeably rarer, or is that exactly the intent ("wow" event that shouldn't get stale)?
3. `SolreignAcidMote` entity (in spores.yml) — TimedDespawn lifetime 30s vs the event's 60-120s duration: motes despawn well before the event's all-clear on the long end. Is that a problem, or is "the motes clear before the official all-clear, on a longer storm" an acceptable/expected reading (real weather often visually calms before an official all-clear is announced), given I deliberately chose "one scatter at start, no continuous respawn" per your own recommendation to keep ActiveTick empty and avoid a spam budget?
4. Confirm `ForceEndSelf(uid, gameRule)` called BEFORE `RaiseNetworkEvent`/`PlayLine`/spawn calls in `Started()` actually prevents those side effects from running (i.e. `ForceEndSelf` doesn't just schedule an end for later while allowing the rest of `Started()` to execute this same call) — I have an integration test asserting `IsGameRuleActive<SolreignAcidStormRuleComponent>()` is false afterward and it passes, but wanted your read on whether `ForceEndSelf` could still let queued side effects (announcement dispatch from the base `Added`, which runs earlier in the lifecycle) leak through even though the rule itself stops.
5. Tone check the actual final loc lines in the diff (station_identity.ftl) against your own corporate-euphemism guidance ("external atmospheric contaminant event", "neutralizing agents deployed", never melting/burning imagery).
6. `SolreignAcidStormRuleComponent`'s `[Access(typeof(SolreignAcidStormRule))]` + `[DataField] EntProtoId MoteEffectPrototype = "SolreignAcidMote"` — same idiom as the sibling `SolreignSporeDriftRuleComponent`. Anything wrong with the reuse?
7. Any other correctness bug, reuse/simplification opportunity, or missed edge case you see fresh in the actual code (not just the spec) — e.g. does spawning up to 20 motes (PreferredMoteCount) per event risk any performance concern given each has a PointLight, same question the sibling SporeDrift already accepted at 14?

Be concrete and terse — cite exact line/hunk if you flag something. I will fix real findings before committing.
