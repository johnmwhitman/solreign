# LICENSE REGEN — Wave 7b (last open license item) — 2026-07-20

Branch `feat/license-hud-themes` off `master` `1a232c2cfe`. Worktree
`~/AI/solreign-trees/license-w7b` (fresh, isolated from the occupied
checkout at `~/AI/Kolton-SS14/server`, per rail).

## Verdict: no-op — the target was already closed by an intervening batch

This wave was dispatched to add license declarations to the sibling HUD
themes (Plasmafire, Slimecore, etc.) that a prior wave-7 attempt reported as
undeclared. Between that report and this dispatch, two intervening batches
already closed the gap on `master`:

- `HUD-THEMES-GEN-2026-07-16.md` (commits `3df3c923c5`, `9d1f52d46b`,
  `9a0514adbd`, `01a79e8afd`, `5dc7e08fd3`, `481d30f326`) — procedurally
  regenerated all six themes (232 files) and added one `meta.json` per theme
  directory, `"license": "CC0-1.0"`, mirroring the wave-6 `Interface/Default`
  precedent. Build/test evidence in that receipt: 0 errors, Content.Tests
  2216/0/3, Solreign integration 302/0/1 (x2), YAML linter clean.
- `HUD-THEME-TEST-2026-07-17.md` — added
  `Content.IntegrationTests/Tests/_Solreign/HudThemeResolutionTest.cs` (5
  tests) pinning theme-directory contents against their `meta.json` manifests
  (the manifest doubles as the CC0 receipt), deleted 2 orphan files, fixed a
  stale `Default/meta.json` state list. 5/5 green, full Solreign suite green.

Both commit sets are ancestors of current `master` HEAD (`1a232c2cfe`,
verified via `git merge-base --is-ancestor`), so this is not staged/unmerged
work — it is fully landed. No file changes were needed or made this wave.

## (1) Census: remaining CC-BY-NC declarations on current master

Method: `license:`/`"license"` field parse (not prose-substring grep, which
false-positives on historical "replaces earlier NC art" commentary), over
every `attributions.yml` and `meta.json` under `Resources/`.

```
grep -rIE '"license"\s*:\s*"[^"]*NC'  --include=meta.json        -r .  → 0
grep -rIE 'license:\s*CC-BY-NC'       --include=attributions.yml -r .  → 0
```

**0 remaining CC-BY-NC license declarations repo-wide** (outside
`Resources/Audio`, untouched per rail). Matches the receipt's own delta
table (`0` before/after on the strict meta.json NC grep) and confirms the
prior wave-7 report's claim that the four texture buckets — Tiles,
`Tiles/Planet/Concrete` (subtree of Tiles), LobbyScreens, Parallaxes — were
already closed by intervening batches:

| Bucket | Status |
|---|---|
| `Resources/Textures/Tiles/attributions.yml` | CC0, procedural (batch-bc); one prose mention of "replaces earlier ... NC art" — historical, not a live declaration |
| `Resources/Textures/Tiles/Planet/Concrete` | covered by Tiles' attributions.yml |
| `Resources/Textures/LobbyScreens/attributions.yml` | no NC hits at all |
| `Resources/Textures/Parallaxes/attributions.yml` | CC0, procedural (batch-c); same historical prose pattern |

Confirmed, not redone.

## (2) Per-theme provenance verdict

Enumerated every subdirectory of `Resources/Textures/Interface/` that is a
player-selectable HUD theme (has its own `SlotBackground.png`/`Slots/`/
`Storage/` set, distinct from shared-icon folders like `Alerts`, `Misc`,
`VerbIcons`, which follow normal per-`.rsi` licensing and were not in scope):

| Theme | Declaration found | Provenance | Verdict / action |
|---|---|---|---|
| Default | `meta.json`, CC0-1.0 | goonstation-family, regenerated wave 6 | already closed (out of this wave's scope, confirmed present) |
| Ashen | `meta.json`, CC0-1.0 | goonstation-family, regenerated `HUD-THEMES-GEN` 07-16 | **already declared — no action** |
| Clockwork | `meta.json`, CC0-1.0 | goonstation-family, regenerated `HUD-THEMES-GEN` 07-16 | **already declared — no action** |
| Minimalist | `meta.json`, CC0-1.0 | goonstation-family, regenerated `HUD-THEMES-GEN` 07-16 | **already declared — no action** |
| Plasmafire | `meta.json`, CC0-1.0 | goonstation-family, regenerated `HUD-THEMES-GEN` 07-16 | **already declared — no action** |
| Retro | `meta.json`, CC0-1.0 | goonstation-family, regenerated `HUD-THEMES-GEN` 07-16 | **already declared — no action** |
| Slimecore | `meta.json`, CC0-1.0 | goonstation-family, regenerated `HUD-THEMES-GEN` 07-16 | **already declared — no action** |
| Nano | `attributions.yml`, CC-BY-SA-3.0 | pre-existing legitimate third-party attribution (Janet Blackquill / sowelipililimute) | **not NC, not undeclared — no action** |

No theme was traced to a fresh space-wizards/RobustToolbox upstream import
requiring an MIT-style declaration to be added, and none required a
goonstation-regen flag — every candidate this wave was already resolved.

## (3) Rails honored

- `Resources/Audio` untouched (verified: no diff, no reads that would matter —
  this wave made no file changes at all).
- No art generation performed (nothing needed generating).
- YAML/JSON spot-check: 9 `attributions.yml` files under
  `Resources/Textures/{Interface,Tiles,Parallaxes}` parsed clean with
  `python3 -c 'import yaml'`; 7 theme `meta.json` files (Default + the six
  regenerated themes) parsed clean with `python3 -c 'import json'`.
- No merge, no push. Commit ends with the required co-author trailer.
- `feat/license-attributions-w7` (stale empty branch, old master) left alone
  per orders — orchestrator to delete.

## Bottom line

Wave 7b — the last open license item — closes as **verified-already-closed**.
Repo-wide CC-BY-NC census is 0 outside Audio; every sibling HUD theme named
in the dispatch (and `Default`/`Nano` alongside them) already carries a
correct, tested license declaration landed by `HUD-THEMES-GEN-2026-07-16`
and hardened by `HUD-THEME-TEST-2026-07-17`. No further license-attribution
work remains on this front.
