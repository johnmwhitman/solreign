<controls:FancyWindow xmlns="https://spacestation14.io"
                      xmlns:gfx="clr-namespace:Robust.Client.Graphics;assembly=Robust.Client"
                      xmlns:controls="clr-namespace:Content.Client.UserInterface.Controls"
                      Title="{Loc 'solreign-oracle-window-title'}"
                      SetSize="380 220"
                      MinSize="320 190">
    <!-- Deliberately small and single-purpose: type a petition, send it, done. The Oracle's
         reply is NOT rendered here — it arrives later as a server-driven popup on the player
         (see SolreignOracleBoundUserInterface's doc comment) — so this window has no reply
         area, no history, no state push to react to. Layout otherwise copies the Contracts
         Board window's panel idiom (SolreignContractsBoardMenu.xaml). -->
    <BoxContainer Orientation="Vertical"
                  Margin="10"
                  SeparationOverride="6"
                  VerticalExpand="True">
        <PanelContainer VerticalExpand="True" HorizontalExpand="True">
            <PanelContainer.PanelOverride>
                <gfx:StyleBoxFlat BackgroundColor="#1B1B1E" />
            </PanelContainer.PanelOverride>
            <BoxContainer Orientation="Vertical" Margin="10" SeparationOverride="6">
                <Label Name="FlavorLabel"
                       Text="{Loc 'solreign-oracle-window-flavor'}"
                       Align="Center" />
                <LineEdit Name="MessageInput" HorizontalExpand="True" />
                <BoxContainer Orientation="Horizontal">
                    <Label Name="CharCountLabel" HorizontalExpand="True" />
                    <Button Name="SendButton" Text="{Loc 'solreign-oracle-ui-send-button'}" />
                </BoxContainer>
                <Label Name="SentLabel"
                       Text="{Loc 'solreign-oracle-window-sent'}"
                       Visible="False" />
            </BoxContainer>
        </PanelContainer>
        <!-- Footer -->
        <BoxContainer Orientation="Vertical">
            <PanelContainer StyleClasses="LowDivider" />
            <BoxContainer Orientation="Horizontal" Margin="10 2 5 0" VerticalAlignment="Bottom">
                <Label Text="{Loc 'solreign-oracle-window-hint'}" StyleClasses="WindowFooterText" />
            </BoxContainer>
        </BoxContainer>
    </BoxContainer>
</controls:FancyWindow>
