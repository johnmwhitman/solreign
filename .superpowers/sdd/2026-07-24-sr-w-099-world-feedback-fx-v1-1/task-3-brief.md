## Task 3 — Production-helper performance proof

**Files**

- Add the narrow `Content.Benchmarks` internal-access declaration only if the current project
  layout requires it.
- Add a uniquely named World Feedback benchmark under the revalidated
  `Content.Benchmarks/_Solreign/FX/Consumers/` layout.
- Add durable benchmark evidence under this branch's receipt subtree.

**Acceptance**

- Benchmark the released v1 positive-entry scan and the v1.1 production classifier on 1-, 3-, and
  8-entry physical, non-kinetic, and mixed inputs.
- Separately compare disabled handler, observe-only cached counter increment, and delivery-candidate
  paths.
- Require 0 B/op for the classifier. Report observer overhead separately.
- A classifier regression over 15 percent blocks completion absent a written integrator exception.
- Do not import or depend on the parked Effect Observatory branch.
